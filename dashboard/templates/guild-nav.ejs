<meta name="viewport" content="width=device-width, initial-scale=1.0">

<div class="card-header" style="padding: 2px; background-color:#0DAED6; border: 0px;">
	<ul class="nav nav-tabs">
		<li class="nav-item">
			<a class="nav-link<% if(active === 'manage') { %> active<% } %>" href="/dashboard/<%= guild.id %>/manage">Ayarlar</a>
		</li>
		<li class="nav-item">
			<a class="nav-link<% if(active === 'stats') { %> active<% } %>" href="/dashboard/<%= guild.id %>/stats">İstatistikler</a>
		</li>
	</ul>
</div>